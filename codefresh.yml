version: '1.0'
steps:
  build:
    title: Build Docker 
    type: build 
    image_name: iwankaramazow/fastreplacestring
    dockerfile: Dockerfile

  tests:
    image: ${{build_step}}
    working_directory: IMAGE_WORK_DIR
    commands:
      - npm run test 
